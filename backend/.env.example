# Server
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=orlando-dev-secret-change-me
JWT_EXPIRES_IN=7d

# SQL Server (separate fields)
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=Orlando
DB_USER=sa
DB_PASSWORD=Your_password123
DB_ENCRYPT=true
DB_TRUST_SERVER_CERTIFICATE=true

# Built for Prisma CLI — keep in sync with fields above
DATABASE_URL="sqlserver://localhost:1433;database=Orlando;user=sa;password=Your_password123;encrypt=true;trustServerCertificate=true"
